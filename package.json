{
  "name": "automacao_precificacao_produtos",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prebuild": "if exist build rmdir /s /q build",
    "build": "tsc",
    "postbuild": "copy src\\appsscript.json build\\",
    "push": "clasp push -f",
    "deploy": "npm run build && npm run push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@google/clasp": "^3.1.3",
    "@types/google-apps-script": "^2.0.8",
    "typescript": "^5.9.3"
  }
}
